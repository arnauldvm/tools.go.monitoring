These files are provided to make possible to test the program on non-Linux systems (Windows, OSX)
To enable test mode, set the FS_ROOT environment variable, for example like this:
$ FS_ROOT=.samples go run main.go

